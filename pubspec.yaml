name: webp_converter
description: Application to convert images to WebP images
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  get_it: ^8.0.3
  path_provider: ^2.1.5
  flutter_bloc: ^8.1.6
  equatable: ^2.0.7
  injectable: ^2.5.0
  url_launcher: ^6.3.1
  file_picker: ^8.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.13
  injectable_generator: ^2.6.2

flutter:
  uses-material-design: true